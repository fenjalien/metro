#import "defs/units.typ"
#import "defs/prefixes.typ"

#import "metro.typ": num, unit, qty, metro-setup, declare-unit, declare-prefix, create-prefix, declare-power, declare-qualifier, metro-reset, num-list, num-product, num-range

#let numlist = num-list
#let numproduct = num-product
#let numrange = num-range